# XML Reader

I made this simple xml reader library so people don't have to use raw org.w3c.dom again.
This library is packaged as jar.

## Project
You can type `clear && ./clean.sh && ./build.sh && ./run.sh` to run the project with the test app module. 
